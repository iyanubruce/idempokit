{
  "name": "redis",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc --build",
    "build:watch": "tsc --build --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build && npm run test && npm run lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@idempotkit/core": "file:../../core",
    "ioredis": "^5.9.2"
  },
  "devDependencies": {
    "@types/ioredis": "^5.0.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
